export interface Warn {
  alertId: string;
  title: string;
  imageUrl: string;
  city: string;
  createdAt: string;
  updatedAt: string;
}

export interface FullWarn {
  alertId: string;
  title: string;
  imageUrl: string;
  body: string;
  city: string;
  createdAt: string;
  updatedAt: string;
}
